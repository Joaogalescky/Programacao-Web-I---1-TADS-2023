let cores = [];

// Adicionando elementos ao vetor
cores.push("vermelho");
cores.push("azul");
cores.push("verde");

console.log(cores);

//Neste exemplo, começamos com um vetor vazio e usamos o método push para adicionar elementos a ele, criando assim um vetor com elementos.