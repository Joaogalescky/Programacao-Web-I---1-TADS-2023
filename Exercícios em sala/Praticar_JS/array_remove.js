let numeros = [1, 2, 3, 4, 5];

// Removendo o último elemento do vetor
numeros.pop();

// Removendo o primeiro elemento do vetor
numeros.shift();

console.log(numeros);

//O método pop remove o último elemento do vetor, e o método shift remove o primeiro elemento do vetor.